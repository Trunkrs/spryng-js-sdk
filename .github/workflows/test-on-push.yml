name: CI
on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: install
        run: yarn
      - name: lint
        run: yarn lint
      - name: test
        run: yarn test
